<div class="text-xs text-base-content/60 space-y-2">
  <div>
    <strong class="block uppercase tracking-wide text-base-content/50 text-[10px] mb-0.5">Navigation</strong>
    <code class="bg-base-200 px-1 py-0.5 rounded">[CHILD_PAGES]</code>
    <code class="bg-base-200 px-1 py-0.5 rounded">[CHILD_PAGES_2_COL]</code>
    — list of visible child pages (1-col / 2-col grid).
  </div>

  <div>
    <strong class="block uppercase tracking-wide text-base-content/50 text-[10px] mb-0.5">Customers &amp; pipeline</strong>
    <code class="bg-base-200 px-1 py-0.5 rounded">[CUSTOMER_PIPELINE:attribute_key]</code>
    customers grouped by a single-select attribute ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[RETENTION_COHORT:attribute_key]</code>
    stage progression over time ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[CUSTOMER_CONCENTRATION]</code>
    top 10 customers by active MRR.
  </div>

  <div>
    <strong class="block uppercase tracking-wide text-base-content/50 text-[10px] mb-0.5">Revenue</strong>
    <code class="bg-base-200 px-1 py-0.5 rounded">[MONTHLY_REVENUE]</code>
    cash collected per month, with event annotations ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[MRR_WALK]</code>
    new + expansion vs contraction + churn ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[NRR_GRR]</code>
    net + gross revenue retention ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[QUICK_RATIO]</code>
    gain ÷ loss (≥4 healthy).
  </div>

  <div>
    <strong class="block uppercase tracking-wide text-base-content/50 text-[10px] mb-0.5">Churn &amp; retention</strong>
    <code class="bg-base-200 px-1 py-0.5 rounded">[CHURN_RATE]</code>
    monthly logo-churn % ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[CHURN_REASONS]</code>
    categorized why-they-left bar chart ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[CHURNED_CUSTOMERS]</code>
    chronological churn log ·
    <code class="bg-base-200 px-1 py-0.5 rounded">[COHORT_RETENTION]</code>
    classical % retained per acquisition cohort.
  </div>
</div>
